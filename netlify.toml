[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Updated to Node 22 to support modern Next.js features
  NODE_VERSION = "22"
  # Prevent sharp from using system libvips that may be incompatible on Netlify build images
  SHARP_IGNORE_GLOBAL_LIBVIPS = "1"
  # Ensure Prisma generates binaries for Netlify's Linux environment
  PRISMA_CLI_BINARY_TARGETS = "debian-openssl-3.0.x"
  # Prevent Prisma from auto-installing/querying during the build step
  # (we already run "prisma generate" in the build script)
  PRISMA_GENERATE_SKIP_AUTOINSTALL = "true"
  # Ensure npm installs optional dependencies (platform-specific binaries)
  NPM_FLAGS = "--include=optional"

# Explicitly use the project's @netlify/plugin-nextjs for consistent Next.js 16 support
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Ensure Prisma client, WASM query engine, and pg adapter are bundled into serverless functions
[functions]
  included_files = [
    "node_modules/.prisma/client/**/*",
    "node_modules/@prisma/client/**/*",
    "node_modules/@prisma/client-runtime-utils/**/*",
    "node_modules/@prisma/adapter-pg/**/*",
    "node_modules/pg/**/*",
    "node_modules/pg-pool/**/*"
  ]

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"

# API cache control
[[headers]]
  for = "/api/*"
  [headers.values]
    Cache-Control = "no-store, no-cache, must-revalidate"

# Static assets cache
[[headers]]
  for = "/_next/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
